#!/bin/bash
#big thanks to ptitSeb for creating box86: https://github.com/ptitSeb/box86

#if box86 is installed then ask if you want to reinstall, if box86 isn't installed, proceed with install.
if
	if
		#is box86 installed? (search for ~/box86 folder)
		
		
	then
		#ask if you want to reinstall, if no, exit both loops
	
	
	
then
	#install box86 (maybe add this?: https://archive.org/details/box86.7z_20200928)
	sudo dpkg -i box86-armhf.deb